import React from 'react';

const Loading = () => {
    return (
        <h2 className='text-6xl text-blue-600 text-center font-semibold animate-bounce'>
            
        </h2>
    );
};

export default Loading;